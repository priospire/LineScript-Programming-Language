operator +(a: i64, b: i64) -> i64 do
  // Avoid recursive '+' in the override body.
  return a - (-b) - (-100)
end

main() -> i64 do
  println(2 + 3)
  println(10 + -2)
  println(3 * 4)
  return 0
end
